deployAs:
  - name: extension
    applicationType: service
    endpoint: /
    scripts:
      postDeploy: npm install && npm run
    configuration:
      securedConfiguration:
        - key: STRIPE_SECRET_KEY
          description: Escaped JSON object
  - name: notification
    applicationType: service
    endpoint: /
    scripts:
      postDeploy: npm install && npm run
    configuration:
      securedConfiguration:
        - key: STRIPE_SECRET_KEY
          description: Escaped JSON object
      
  